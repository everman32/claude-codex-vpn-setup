# Deny the entire repository by default. Only files required to build the
# development image are sent to the Docker builder.
**

!Dockerfile
!scripts/
!scripts/entrypoint.sh
!scripts/vpn-dns.sh
!scripts/vpn-healthcheck.sh

!scripts/ai-sandbox-check.sh
!scripts/claude-managed-settings.json
